[
  {
    "id": "ppl-sprint1-week3-ir",
    "title": "Individual Review Sprint 1 Week 3: Bukti Kompetensi PPL",
    "date": "2026-03-09",
    "category": "PPL IR",
    "tags": ["PPL", "IR", "Sprint 1", "SIRA", "Kompetensi"],
    "excerpt": "Dokumentasi lengkap bukti kompetensi Individual Review (IR) Sprint 1 Week 3 — mencakup Part A (Cognitive) dan Part B (Best Practice) untuk proyek SIRA.",
    "readTime": "12 min read",
    "content": "<h2>Tentang Post Ini</h2><p>Post ini adalah dokumentasi lengkap bukti kompetensi untuk <strong>Individual Review (IR) Sprint 1, Week 3</strong> mata kuliah Proyek Perangkat Lunak (PPL) di Universitas Indonesia. Proyek yang dikerjakan: <strong>Smart Invoice Reminder AI (SIRA)</strong> untuk PT Nashta.</p><p>Untuk penjelasan teknis mendalam tentang implementasi TDD, SOLID, dan refactoring, lihat: <a href='/blog/ppl-sprint1-week3'>Blog Post Teknis: Refactoring Payment Service</a></p><hr/><h2>Part B — Best Practice</h2><h3>B1. Test Driven Development (TDD, Code Coverage)</h3><p><strong>Claim: Score 3</strong></p><p>Mengerjakan refactoring code quality untuk fitur Payment Recording (SIRA-31). Total <strong>24 unit tests</strong> mencakup seluruh layer (router, service, DB).</p><ul><li><strong>Positive tests:</strong> 8 test cases — GET list, GET by ID, create payment (days_late calculation, PAID status, PARTIAL status), update payment (200, recalculate status, recalculate days_late)</li><li><strong>Negative tests:</strong> 7 test cases — 404 not found, 400 paid invoice guard, 401 unauthenticated, 400 no fields</li></ul><p><strong>F.I.R.S.T. principles:</strong> 24 tests selesai dalam 0.07s (Fast), setiap test independent, mock data konsisten (Repeatable), assert langsung (Self-validating), ditulis bersamaan kode (Timely).</p><p><strong>Test isolation:</strong> <code>AsyncMock</code> + <code>patch()</code> untuk isolasi Supabase dependency. Fixture <code>admin_client</code> dan <code>client</code> mock auth dependency.</p><p><strong>Evidence:</strong></p><ul><li>MR !53: <code>https://gitlab.lab.local/.../SIRA/-/merge_requests/53</code></li><li>Test file: <code>apps/api/tests/test_payments.py</code></li></ul><hr/><h3>B2. Programming (Best Practice, OO, SOLID)</h3><p><strong>Claim: Score 3</strong></p><p>Menerapkan refactoring berdasarkan prinsip SOLID pada MR !53:</p><ul><li><strong>SRP:</strong> Extracted <code>_recalculate_invoice_status()</code> — method khusus recalculate status invoice. Extracted <code>_to_response()</code> — static method konversi dict → PaymentResponse.</li><li><strong>DRY:</strong> 2 blok identik dikonsolidasi ke 1 shared method.</li><li><strong>Clean Architecture:</strong> Router (HTTP) → Service (business logic) → DB Queries (persistence).</li><li><strong>DIP:</strong> Router bergantung pada <code>PaymentService</code> (abstraksi), DB client di-inject via <code>Depends(get_db)</code>.</li></ul><p><strong>Evidence:</strong></p><ul><li>Commit <code>da41cdc</code>: <code>https://gitlab.lab.local/.../SIRA/-/commit/da41cdc</code></li><li>MR !53 diff: <code>https://gitlab.lab.local/.../SIRA/-/merge_requests/53/diffs</code></li></ul><hr/><h3>B3. Development Discipline (Git Flow, Commit Messages)</h3><p><strong>Claim: Score 3</strong></p><ul><li><strong>Conventional Commits:</strong> <code>type(scope): description</code></li><li><strong>Branch naming:</strong> <code>dafandikri/refactor/SIRA-31-payment-code-quality</code>, <code>dafandikri/fix/SIRA-64-make-dev-termination</code></li><li><strong>MR sebelum merge:</strong> MR !50 (merged), MR !53 (open) — keduanya dengan Summary + Test Plan</li><li><strong>Quality gates:</strong> Pre-commit (Biome, Ruff, Knip) + Pre-push (tsc, Vitest, Mypy, Pytest)</li></ul><p><strong>Evidence:</strong></p><ul><li>MR !50: <code>https://gitlab.lab.local/.../SIRA/-/merge_requests/50</code></li><li>MR !53: <code>https://gitlab.lab.local/.../SIRA/-/merge_requests/53</code></li></ul><hr/><h3>B4. Team Development Management (Issue Tracking, Code Review)</h3><p><strong>Claim: Score 3</strong></p><ul><li><strong>Issue tracking:</strong> Linear issues SIRA-31, SIRA-64. Setiap MR di-tag dengan ticket.</li><li><strong>Code review anggota tim:</strong><ul><li>Review MR !49 (Rifqi — overdue detection): identifikasi konflik arsitektur dengan MR !43</li><li>Review MR !43 (Nadhif — overdue detection): identifikasi duplikasi, rekomendasikan konsolidasi</li><li>Review MR !50 (Abhipraya — process-compose): terima perubahan setelah diskusi</li></ul></li></ul><p><strong>Evidence:</strong> Review comments di MR !49, !43, !50</p><hr/><h3>B5. Code Quality (SonarQube)</h3><p><strong>Claim: Score 3</strong></p><p>SonarQube mendeteksi 5 issues pada payment code, semua diperbaiki di MR !53:</p><ol><li><strong>Code duplication</strong> — identical recalculation logic → extracted ke <code>_recalculate_invoice_status()</code></li><li><strong>Code duplication</strong> — identical PaymentResponse construction → extracted ke <code>_to_response()</code></li><li><strong>Dead code</strong> — <code>MOCK_STAFF_USER</code> tidak digunakan → dihapus</li><li><strong>Stale file</strong> — duplikat test file → dihapus</li><li><strong>Missing response_model</strong> — POST endpoint → ditambahkan</li></ol><p>Setelah fix: Ruff clean, Mypy clean, 24 tests pass, 0 SonarQube issues.</p><p><strong>Evidence:</strong> MR !53</p><hr/><h3>B6. Security Awareness</h3><p><strong>Claim: Score 3</strong></p><ul><li><strong>Auth guards:</strong> GET dilindungi <code>get_current_user</code> (JWT), PUT dilindungi <code>require_admin</code></li><li><strong>Business logic:</strong> Guard invoice PAID, Pydantic <code>Field(gt=0)</code> mencegah payment negatif</li><li><strong>SAST pipeline:</strong> <code>bandit</code> (Python SAST) + <code>pnpm audit</code> (dependency scan) di GitLab CI</li></ul><p><strong>Evidence:</strong> MR !34, !38, test <code>test_update_payment_returns_401_for_unauthenticated</code></p><hr/><h3>B7. AI Literacy</h3><p><strong>Claim: Score 3</strong></p><p>Menggunakan <strong>Claude Code</strong> (Anthropic CLI agent) secara ekstensif:</p><ul><li><strong>Code refactoring:</strong> Analisis issues, identifikasi duplicated logic, eksekusi refactoring</li><li><strong>Code review:</strong> Review MR !49, !43 — identifikasi cross-MR conflict</li><li><strong>Git operations:</strong> MR creation via glab CLI, branch management</li><li><strong>DevOps:</strong> Debug sync script, process-compose config</li></ul><p><strong>Evidence:</strong> <code>Co-Authored-By: Claude Opus 4.6</code> pada commit messages, MR !53</p><hr/><h2>Part A — Cognitive Competences: Mandatory</h2><h3>A1. Software Testing / TDD (CPL 6 IAF)</h3><p><strong>Claim: Score 3</strong></p><p>Pemahaman mendalam TDD melalui Payment Recording (SIRA-31):</p><ul><li>24 tests mencakup semua layer (router, service, DB) — positive + negative cases</li><li>F.I.R.S.T. principles diterapkan konsisten</li><li>Mock/stub: <code>MagicMock(spec=Client)</code>, <code>AsyncMock</code>, <code>patch()</code>, fixture dependency override</li></ul><p><strong>Evidence:</strong> MR !53, <code>tests/test_payments.py</code></p><hr/><h3>A2. Best Practice Programming / SOLID (CPL 6 AA)</h3><p><strong>Claim: Score 3</strong></p><ul><li><strong>SRP:</strong> PaymentService = business logic, Router = HTTP, DB queries = persistence</li><li><strong>OCP:</strong> Service layer extensible tanpa ubah router. Pydantic models extensible via inheritance</li><li><strong>DIP:</strong> Router → PaymentService (abstraksi), inject via <code>Depends(get_db)</code></li><li><strong>Design Pattern:</strong> Service Layer (Router → Service → DB)</li></ul><p><strong>Evidence:</strong> MR !53, <code>payment_service.py</code>, <code>payments.py</code></p><hr/><h3>A3. Static Analysis / Quality Assurance (CPL 6 MRA)</h3><p><strong>Claim: Score 3</strong></p><p>Tools: SonarQube (GitLab CI, MR !26), Ruff (linter/formatter), Mypy (type checker), Biome (frontend), Knip (dead code).</p><p>Workflow: SonarQube deteksi issues → MR !53 fix → 5 pre-commit checks + 4 pre-push checks pass.</p><p><strong>Evidence:</strong> MR !53, SonarQube dashboard</p><hr/><h3>A4. Teamwork with Tools (CPL 2 IAR)</h3><p><strong>Claim: Score 3</strong></p><p>Tools kolaborasi: GitLab (MR, code review), Linear (issue tracking), glab CLI, Claude Code (AI pair programming).</p><p>MR best practices: Summary + Test Plan, branch naming convention, conventional commits, pre-merge hooks.</p><p><strong>Evidence:</strong> MR !50, !53</p><hr/><h3>A5. Platform Monitoring (CPL 6 DA)</h3><p><strong>Claim: Score 3</strong></p><ul><li><strong>GlitchTip</strong> — self-hosted error tracking (MR !33) + email alerting via Resend (MR !39)</li><li><strong>Structured JSON Logging</strong> — HTTP access logging middleware (MR !37): method, path, status, duration</li></ul><p><em>Note: Beberapa commit tercatat di bawah nama lain karena squash merge saat migrasi GitHub → GitLab.</em></p><p><strong>Evidence:</strong> MR !37, !39, !33</p><hr/><h3>A6. Secure Programming (CPL 6 ES)</h3><p><strong>Claim: Score 3</strong></p><ol><li>Payment endpoints awalnya tanpa auth guard → ditambahkan <code>get_current_user</code> (MR !34)</li><li>PUT payment khusus admin → <code>require_admin</code></li><li>Pydantic <code>Field(gt=0)</code> mencegah negative amounts</li><li>JWT verification via JWKS (ES256)</li></ol><p><strong>Evidence:</strong> MR !34 (auth guard), <code>payments.py</code></p><hr/><h2>Part A — Cognitive Competences: Elective (4 dipilih)</h2><h3>Elective 2: Software Deployment &amp; Environment (CPL 6 DA)</h3><p><strong>Claim: Score 3</strong></p><ul><li>Docker containerization untuk semua services (API, Web, Redis, Supabase)</li><li>GitLab CI/CD: lint → test → type-check → build → deploy</li><li>Deploy via SSH ke VPS (Tailscale network)</li><li>Nginx reverse proxy: <code>api.sira.nashta.co.id</code>, <code>sira.nashta.co.id</code></li><li><code>process-compose</code> untuk unified local dev</li></ul><p><strong>Evidence:</strong> MR !44 (Nginx), !25 (GitLab CI), !50 (process-compose)</p><hr/><h3>Elective 3: Refactoring &amp; Design Pattern (CPL 6 AA)</h3><p><strong>Claim: Score 3</strong></p><p>MR !53 menerapkan refactoring Martin Fowler:</p><ul><li><strong>Extract Method:</strong> <code>_recalculate_invoice_status()</code> dan <code>_to_response()</code></li><li><strong>Service Layer Pattern:</strong> Router → Service → DB</li><li><strong>DTO:</strong> Pydantic models (PaymentCreate, PaymentUpdate, PaymentResponse)</li></ul><p><strong>Evidence:</strong> MR !53 diff</p><hr/><h3>Elective 5: More Testing — SAST, Security Testing (CPL 6 IAF)</h3><p><strong>Claim: Score 3</strong></p><ul><li>GitLab SAST terintegrasi CI pipeline (MR !38)</li><li><code>bandit</code> — Python security analysis (SQL injection, hardcoded passwords)</li><li><code>pnpm audit</code> — dependency vulnerability scanning</li><li>Berjalan otomatis pada setiap push/MR</li></ul><p><strong>Evidence:</strong> MR !38, <code>.gitlab-ci.yml</code> security stage</p><hr/><h3>Elective 7: Automatic Data Seeding/Migration (CPL 6 MRA)</h3><p><strong>Claim: Score 3</strong></p><ul><li>Supabase migrations: <code>supabase/migrations/</code></li><li>Rollback SQL untuk setiap migration (MR !41)</li><li>Auto-migration di CI/CD deploy pipeline (MR !42)</li><li>Dev seed: <code>supabase/seed.sql</code></li><li>Production seed: admin + staff users (MR !47)</li></ul><p><strong>Evidence:</strong> MR !41, !42, !47</p>"
  },
  {
    "id": "ppl-sprint1-week3",
    "title": "Refactoring Payment Service: SOLID, TDD, dan Code Quality dalam Proyek SIRA",
    "date": "2026-03-08",
    "category": "Software Engineering",
    "tags": ["PPL", "TDD", "SOLID", "SonarQube", "FastAPI", "Python"],
    "excerpt": "Bagaimana saya menerapkan prinsip SOLID, TDD, dan static analysis tools untuk meningkatkan kualitas kode payment recording di proyek Smart Invoice Reminder AI.",
    "readTime": "8 min read",
    "content": "<h2>Konteks: Smart Invoice Reminder AI (SIRA)</h2><p>Proyek SIRA adalah sistem manajemen invoice berbasis AI yang dibangun untuk PT Nashta. Sebagai bagian dari mata kuliah Proyek Perangkat Lunak (PPL) di Universitas Indonesia, saya bertanggung jawab atas fitur <strong>Payment Recording</strong> (SIRA-31) — memungkinkan user mencatat pembayaran terhadap invoice, dengan kalkulasi otomatis status invoice (UNPAID → PARTIAL → PAID) dan keterlambatan pembayaran.</p><p>Minggu ini, setelah fitur di-merge ke main, SonarQube mendeteksi beberapa code quality issues. Alih-alih hanya \"fix cepat\", saya menjadikan ini kesempatan untuk menerapkan refactoring yang bermakna.</p><hr/><h2>1. Test-Driven Development: 24 Tests, 0.07 Detik</h2><p>Sebelum menyentuh kode produksi, saya memastikan test suite yang solid sudah ada. Payment recording memiliki <strong>24 unit tests</strong> yang mencakup seluruh layer arsitektur:</p><h3>Positive Tests (Happy Path)</h3><pre><code>def test_create_payment_calculates_days_late_correctly(client):\n    \"\"\"payment_date=2026-02-05, due_date=2026-01-31 → days_late=5\"\"\"\n    # ... setup mocks ...\n    response = client.post(\"/api/payments/\", json={\n        \"invoice_id\": \"inv-0001\",\n        \"amount_paid\": 500000.00,\n        \"payment_date\": \"2026-02-05\",\n    })\n    assert response.status_code == 201\n    assert response.json()[\"days_late\"] == 5</code></pre><p>Test ini memverifikasi bahwa kalkulasi <code>days_late</code> benar: jika due date 31 Januari dan dibayar 5 Februari, maka terlambat 5 hari.</p><h3>Negative Tests (Error Cases)</h3><pre><code>def test_create_payment_returns_400_for_paid_invoice(client):\n    \"\"\"Cannot record payment on an already-PAID invoice.\"\"\"\n    with patch(\"app.services.payment_service.get_invoice_by_id\",\n               new_callable=AsyncMock, return_value=MOCK_INVOICE_PAID):\n        response = client.post(\"/api/payments/\", json={\n            \"invoice_id\": \"inv-0001\",\n            \"amount_paid\": 500000.00,\n            \"payment_date\": \"2026-02-05\",\n        })\n    assert response.status_code == 400\n    assert \"paid\" in response.json()[\"detail\"].lower()</code></pre><p>Guard ini mencegah duplikasi pembayaran pada invoice yang sudah lunas — penting untuk integritas data finansial.</p><h3>Prinsip F.I.R.S.T. dalam Aksi</h3><table><tr><th>Prinsip</th><th>Implementasi</th></tr><tr><td><strong>Fast</strong></td><td>24 tests selesai dalam 0.07 detik — full mock, tanpa I/O</td></tr><tr><td><strong>Independent</strong></td><td>Setiap test berdiri sendiri, tidak bergantung urutan eksekusi</td></tr><tr><td><strong>Repeatable</strong></td><td>Mock data konsisten (<code>MOCK_PAYMENT</code>, <code>MOCK_INVOICE_UNPAID</code>)</td></tr><tr><td><strong>Self-validating</strong></td><td>Assert langsung pada status code dan response body</td></tr><tr><td><strong>Timely</strong></td><td>Tests ditulis bersamaan dengan implementasi</td></tr></table><h3>Test Isolation: Mock dan Stub</h3><pre><code># Fixture untuk admin client dengan dependency override\n@pytest.fixture\ndef admin_client():\n    mock_db = MagicMock(spec=Client)\n    app.dependency_overrides[get_db] = lambda: mock_db\n    app.dependency_overrides[require_admin] = lambda: MOCK_ADMIN_USER\n    yield TestClient(app)\n    app.dependency_overrides.clear()</code></pre><p>Setiap test menggunakan <code>AsyncMock</code> dan <code>patch()</code> untuk mengisolasi dependency dari Supabase. Ini memastikan test tidak bergantung pada state database external.</p><hr/><h2>2. Refactoring dengan Prinsip SOLID</h2><p>SonarQube mendeteksi <strong>code duplication</strong> di <code>PaymentService</code>. Saya melihat ini sebagai kesempatan menerapkan refactoring Martin Fowler.</p><h3>Masalah: Kode Duplikat</h3><p>Method <code>create()</code> dan <code>update()</code> memiliki blok identik untuk:</p><ol><li>Menghitung total payments dan update status invoice</li><li>Mengonstruksi <code>PaymentResponse</code> dari dict</li></ol><pre><code># SEBELUM: Duplikasi di create() dan update()\nasync def create(self, data):\n    # ... business logic ...\n    all_payments = await get_payments_by_invoice(self.db, invoice_id)\n    total_paid = sum(float(p[\"amount_paid\"]) for p in all_payments)\n    invoice_amount = float(invoice[\"amount\"])\n    new_status = \"PAID\" if total_paid >= invoice_amount else \"PARTIAL\"\n    await update_invoice_status(self.db, invoice_id, new_status)\n    return PaymentResponse(\n        id=str(payment[\"id\"]),\n        invoice_id=str(payment[\"invoice_id\"]),\n        amount_paid=float(payment[\"amount_paid\"]),\n        payment_date=date.fromisoformat(str(payment[\"payment_date\"])),\n        days_late=int(payment[\"days_late\"]),\n    )\n\nasync def update(self, payment_id, data):\n    # ... KODE YANG SAMA persis diulang ...</code></pre><h3>Solusi: Extract Method (SRP + DRY)</h3><pre><code># SESUDAH: Clean, DRY, Single Responsibility\nasync def _recalculate_invoice_status(self, invoice_id: str) -> None:\n    all_payments = await get_payments_by_invoice(self.db, invoice_id)\n    invoice = await get_invoice_by_id(self.db, invoice_id)\n    if invoice is None:\n        return\n    total_paid = sum(float(p[\"amount_paid\"]) for p in all_payments)\n    invoice_amount = float(invoice[\"amount\"])\n    new_status = \"PAID\" if total_paid >= invoice_amount else \"PARTIAL\"\n    await update_invoice_status(self.db, invoice_id, new_status)\n\n@staticmethod\ndef _to_response(record: dict[str, Any]) -> PaymentResponse:\n    return PaymentResponse(\n        id=str(record[\"id\"]),\n        invoice_id=str(record[\"invoice_id\"]),\n        amount_paid=float(record[\"amount_paid\"]),\n        payment_date=date.fromisoformat(str(record[\"payment_date\"])),\n        days_late=int(record[\"days_late\"]),\n    )</code></pre><h3>Prinsip SOLID yang Diterapkan</h3><table><tr><th>Prinsip</th><th>Implementasi</th></tr><tr><td><strong>Single Responsibility</strong></td><td><code>_recalculate_invoice_status()</code> hanya menangani satu hal: recalculate status. <code>_to_response()</code> hanya mapping data.</td></tr><tr><td><strong>Open/Closed</strong></td><td>Service layer extensible tanpa mengubah router. Pydantic models bisa di-extend via inheritance.</td></tr><tr><td><strong>Dependency Inversion</strong></td><td>Router bergantung pada <code>PaymentService</code> (abstraksi), bukan DB queries langsung. DB client di-inject via <code>Depends(get_db)</code>.</td></tr></table><h3>Arsitektur Clean: Router → Service → DB</h3><pre><code># Router: hanya HTTP concerns\n@router.post(\"/\", status_code=201, response_model=PaymentResponse)\nasync def create_payment(\n    data: PaymentCreate,\n    _user: AuthenticatedUser = Depends(get_current_user),\n    db: Client = Depends(get_db),\n) -> PaymentResponse:\n    service = PaymentService(db)\n    try:\n        result = await service.create(data)\n    except ValueError as e:\n        raise HTTPException(status_code=400, detail=str(e)) from e\n    if result is None:\n        raise HTTPException(status_code=404, detail=\"Invoice not found\")\n    return result</code></pre><p>Router hanya mengurus HTTP status codes dan error handling. Semua business logic ada di service layer.</p><hr/><h2>3. Static Analysis: SonarQube + Pre-commit Pipeline</h2><p>Proyek SIRA menggunakan <strong>5 layer quality gates</strong> yang berjalan otomatis:</p><h3>Pre-commit Hooks (5 checks)</h3><ol><li><strong>Biome</strong> — Frontend linter (53 files checked)</li><li><strong>Ruff check</strong> — Python linter (all checks passed)</li><li><strong>Ruff format</strong> — Python formatter (54 files formatted)</li><li><strong>tsc</strong> — TypeScript type checker</li><li><strong>Knip</strong> — Dead code detector</li></ol><h3>Pre-push Hooks (4 checks)</h3><ol><li><strong>tsc --noEmit</strong> — Full type check</li><li><strong>Vitest</strong> — 40 frontend tests</li><li><strong>Mypy</strong> — Python static type analysis</li><li><strong>Pytest</strong> — 71 backend tests (termasuk 24 payment tests)</li></ol><h3>SonarQube Issues yang Diperbaiki</h3><table><tr><th>Issue</th><th>Severity</th><th>Fix</th></tr><tr><td>Duplicated code blocks di <code>create()</code> dan <code>update()</code></td><td>Major</td><td>Extract ke <code>_recalculate_invoice_status()</code></td></tr><tr><td>Duplicated PaymentResponse construction</td><td>Major</td><td>Extract ke <code>_to_response()</code></td></tr><tr><td>Unused variable <code>MOCK_STAFF_USER</code></td><td>Minor</td><td>Dihapus dari test file</td></tr><tr><td>Stale duplicate test file</td><td>Minor</td><td>Dihapus <code>src/app/tests/test_payments.py</code></td></tr><tr><td>Missing <code>response_model</code> pada POST</td><td>Info</td><td>Ditambahkan <code>response_model=PaymentResponse</code></td></tr></table><p>Setelah fix, semua quality gates pass: <strong>0 issues dari SonarQube</strong>.</p><hr/><h2>4. Security: Auth Guards dan SAST</h2><h3>Authentication & Authorization</h3><pre><code># GET — semua authenticated users\n@router.get(\"/\", response_model=list[PaymentResponse])\nasync def list_payments(\n    _user: AuthenticatedUser = Depends(get_current_user),  # JWT auth\n    db: Client = Depends(get_db),\n):\n\n# PUT — hanya admin\n@router.put(\"/{payment_id}\", response_model=PaymentResponse)\nasync def update_payment(\n    payment_id: str,\n    data: PaymentUpdate,\n    _admin: AuthenticatedUser = Depends(require_admin),  # Admin only\n    db: Client = Depends(get_db),\n):</code></pre><p>Setiap endpoint dilindungi auth dependency. PUT payment khusus admin — jika staff mencoba update, mendapat 401.</p><h3>Input Validation</h3><pre><code>class PaymentCreate(BaseModel):\n    invoice_id: str\n    amount_paid: float = Field(gt=0)  # Mencegah nilai negatif/nol\n    payment_date: date</code></pre><h3>SAST Pipeline</h3><p>GitLab CI menjalankan security scanning otomatis:</p><ul><li><strong>Bandit</strong> — mendeteksi SQL injection, hardcoded passwords, dll.</li><li><strong>pnpm audit</strong> — dependency vulnerability scanning</li></ul><hr/><h2>5. Kolaborasi Tim dan Git Flow</h2><h3>Branch Naming Convention</h3><pre><code>dafandikri/feat/SIRA-31-payment-recording\ndafandikri/refactor/SIRA-31-payment-code-quality\ndafandikri/fix/SIRA-64-make-dev-termination</code></pre><h3>Merge Request Best Practices</h3><p>Setiap MR memiliki:</p><ul><li><strong>Summary</strong> — bullet points perubahan</li><li><strong>Test Plan</strong> — checklist verifikasi</li><li><strong>Conventional Commits</strong> — <code>type(scope): description</code></li></ul><h3>Code Review</h3><p>Saya me-review MR dari anggota tim lain:</p><ul><li><strong>MR !49</strong> (Rifqi — overdue detection) — mengidentifikasi konflik arsitektur dengan MR !43</li><li><strong>MR !43</strong> (Nadhif — overdue detection) — menemukan duplikasi implementasi, merekomendasikan konsolidasi</li></ul><hr/><h2>6. AI Literacy: Claude Code sebagai Pair Programmer</h2><p>Seluruh refactoring ini dilakukan menggunakan <strong>Claude Code</strong> (Anthropic CLI agent) sebagai AI pair programmer:</p><ul><li><strong>Code Analysis</strong> — Claude mengidentifikasi duplicated logic dan dead code yang perlu di-refactor</li><li><strong>Refactoring Execution</strong> — Extract method, cleanup unused constants, fix missing annotations</li><li><strong>Test Verification</strong> — Menjalankan 24 tests + lint + type check setelah setiap perubahan</li><li><strong>MR Creation</strong> — Generate MR description dengan summary dan test plan</li><li><strong>Code Review</strong> — Review MR anggota tim lain, mengidentifikasi cross-MR conflicts</li></ul><p>Bukti: Semua commit mengandung <code>Co-Authored-By: Claude Opus 4.6 &lt;noreply@anthropic.com&gt;</code></p><hr/><h2>Refleksi</h2><p>Sprint ini mengajarkan saya bahwa code quality bukan hanya soal \"kode yang jalan\" — tapi soal kode yang <strong>maintainable</strong>, <strong>testable</strong>, dan <strong>secure</strong>. Refactoring sederhana seperti Extract Method bisa mengurangi cognitive complexity secara signifikan dan membuat kode lebih mudah dipahami oleh anggota tim lain.</p><p>Ke depannya, saya ingin lebih mendalami:</p><ul><li>Integration testing dengan Supabase test containers</li><li>Load testing dengan k6 untuk payment endpoints</li><li>Mutation testing untuk memvalidasi kualitas test suite</li></ul>"
  }
]
