# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test artifacts — regenerated by `pnpm test:cov`, so committing them just
# churns the diff on every run.
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env*

# Infrastructure
certs/
# Stray tooling output; also triggers dev-server page reloads when watched.
.lavish/

# Large reference assets — never deployed, never committed.
reference/
